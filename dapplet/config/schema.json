{
    "type": "object",
    "required": [
        "overlayUrl",
        "serverUrl"
    ],
    "properties": {
        "overlayUrl": {
            "type": "string",
            "title": "Overlay URL"
        },
        "serverUrl": {
            "type": "string",
            "title": "Server URL"
        }
    }
}